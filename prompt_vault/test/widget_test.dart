// Basic smoke test for PromptVault app
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('PromptVault smoke test placeholder', (WidgetTester tester) async {
    // Full integration tests require Supabase — skipping for now
    expect(true, isTrue);
  });
}
